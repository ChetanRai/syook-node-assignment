1. API - CRU items {id, name, price}
2. API - CRU delivery vehicles {id, regNo, type, city, activeOrders}
3. API - Create orders {id(auto incr),itemId,price,customerId,deliveryVehicleId,isDelivered, invoiceId}
4. API - Order delivered {}

Customer {id, name, city}

GTH:
error handling
logging
token authentication
invoice generate {customer name, item name & price, invoiceId -> order}


Flow | create order:
1. customer(reg/not)
2. deliveryVehicle(city-customer city, activeOrder < 2)



API - Create Order:
{
	customerDetails: {
		id?: string
		name?: string
		city?: string
	}
	itemId: string
}

{
	customer: {
		name, city, id
	}
	item: {
		name, price
	}
	deliveryVehicle: {
		regNo, type
	}
	invoiceId: string
}

items =>
1. create item
2. get item
3. get all items
4. update item


delivery vehicles =>
1. create item
2. get item
3. get all items
4. update item


orders =>
1. create order [customer creation as well]
2. deliver order
